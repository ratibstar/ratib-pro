<?php
declare(strict_types=1);

/**
 * Logistics routes — Phase 1 bootstrap only (no Admin UI / API routes yet).
 *
 * Future paths (P2+):
 *   /admin/.../logistics
 *   /admin/.../logistics/vehicles|drivers|trips|shipments|routes|expenses|reports
 *   /api/v1/logistics/*
 *
 * @var Rateb\App\Core\Router $router
 */

// Phase 1: module load + permissions/schema only — no routes registered yet.
