# Deny HTTP access to homepage CMS JSON snapshot (same idea as /storage/)
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
