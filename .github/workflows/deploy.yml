name: Deploy PHP App to cPanel

on:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: production-deploy
  cancel-in-progress: false

jobs:
  trigger-cpanel-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout (for deploy marker verification)
        uses: actions/checkout@v4

      - name: Validate cPanel API secrets
        run: |
          if [ -z "${{ secrets.CPANEL_HOST }}" ]; then
            echo "Missing required secret: CPANEL_HOST"
            exit 1
          fi
          if [ -z "${{ secrets.CPANEL_USER }}" ]; then
            echo "Missing required secret: CPANEL_USER"
            exit 1
          fi
          if [ -z "${{ secrets.CPANEL_API_TOKEN }}" ]; then
            echo "Missing required secret: CPANEL_API_TOKEN"
            exit 1
          fi
          if [ -z "${{ secrets.CPANEL_REPO_ROOT }}" ]; then
            echo "Missing required secret: CPANEL_REPO_ROOT"
            exit 1
          fi

      # Webhook: treat any 2xx as success (no curl -f — some hooks return 204/201). Follow redirects (-L).
      # UAPI: cPanel VersionControl often returns HTTP 500 on otherwise healthy hosts — do not fail the whole workflow.
      - name: Deploy to cPanel (webhook, else UAPI)
        id: cpanel_deploy
        continue-on-error: true
        run: |
          HOOK_URL="$(printf '%s' "${{ secrets.CPANEL_DEPLOY_HOOK_URL }}" | tr -d '\r\n' | xargs)"
          if [ -n "$HOOK_URL" ]; then
            echo "Trying cPanel deploy webhook..."
            TMP="$(mktemp)"
            set +e
            HTTP_CODE="$(curl -sS -L --connect-timeout 20 --max-time 180 \
              --retry 3 --retry-delay 3 \
              -o "$TMP" -w '%{http_code}' "$HOOK_URL")"
            CURL_EXIT=$?
            if [ "$CURL_EXIT" -ne 0 ] || [ -z "$HTTP_CODE" ]; then
              HTTP_CODE="000"
            fi
            echo "Webhook HTTP status: ${HTTP_CODE:-000}"
            if [ -s "$TMP" ]; then echo "Webhook body (first 2k):"; head -c 2000 "$TMP" | cat -v; echo; fi
            rm -f "$TMP"
            case "$HTTP_CODE" in
              2??)
                echo "Webhook returned 2xx — will still run VersionControl sync + .cpanel.yml deploy (webhook alone does not rsync to out.ratib.sa)."
                ;;
              *)
                echo "Webhook did not return 2xx (or curl failed); using VersionControl UAPI."
                ;;
            esac
          else
            echo "CPANEL_DEPLOY_HOOK_URL not set; using VersionControl UAPI only."
          fi

          CPANEL_BASE_URL="https://${{ secrets.CPANEL_HOST }}:2083"
          AUTH_HEADER="Authorization: cpanel ${{ secrets.CPANEL_USER }}:${{ secrets.CPANEL_API_TOKEN }}"

          call_cpanel_api() {
            local endpoint="$1"
            shift
            curl -fsSL --get \
              --retry 5 \
              --retry-all-errors \
              --retry-delay 3 \
              --connect-timeout 15 \
              --max-time 90 \
              -H "$AUTH_HEADER" \
              "$@" \
              "${CPANEL_BASE_URL}/execute/${endpoint}"
          }

          CLEAN_REPO_ROOT="$(printf '%s' "${{ secrets.CPANEL_REPO_ROOT }}" | tr -d '\r\n' | xargs)"
          if [ -z "$CLEAN_REPO_ROOT" ]; then
            echo "CPANEL_REPO_ROOT is empty after trimming whitespace."
            exit 1
          fi

          CLEAN_BRANCH="$(printf '%s' "${{ github.ref_name }}" | tr -d '\r\n' | xargs)"
          if [ -z "$CLEAN_BRANCH" ]; then
            CLEAN_BRANCH="main"
          fi

          SYNC_RESPONSE="$(call_cpanel_api "VersionControl/update" \
            --data-urlencode "repository_root=$CLEAN_REPO_ROOT" \
            --data-urlencode "branch=$CLEAN_BRANCH")"

          echo "cPanel sync API response:"
          echo "$SYNC_RESPONSE"

          SYNC_STATUS="$(echo "$SYNC_RESPONSE" | python3 -c "import json,sys; d=json.load(sys.stdin); r=d.get('result',d) or {}; print(r.get('status', d.get('status',0)))")"
          if [ "$SYNC_STATUS" != "1" ]; then
            echo "Repository sync API returned non-success status."
            exit 1
          fi

          RESPONSE="$(call_cpanel_api "VersionControlDeployment/create" \
            --data-urlencode "repository_root=$CLEAN_REPO_ROOT" \
            )"

          echo "cPanel API response:"
          echo "$RESPONSE"

          STATUS="$(echo "$RESPONSE" | python3 -c "import json,sys; d=json.load(sys.stdin); r=d.get('result',d) or {}; print(r.get('status', d.get('status',0)))")"
          LOG_PATH="$(echo "$RESPONSE" | python3 -c "import json,sys; d=json.load(sys.stdin); r=d.get('result',d) or {}; data=(r.get('data', d.get('data',{})) or {}); print(data.get('log_path',''))")"
          REPO_ROOT="$(echo "$RESPONSE" | python3 -c "import json,sys; d=json.load(sys.stdin); r=d.get('result',d) or {}; data=(r.get('data', d.get('data',{})) or {}); print(data.get('repository_root',''))")"

          echo "cPanel deploy status: $STATUS"
          echo "cPanel deploy repository_root: $REPO_ROOT"
          echo "cPanel deploy log_path: $LOG_PATH"

          if [ "$STATUS" != "1" ]; then
            echo "Deployment API returned non-success status."
            exit 1
          fi

          DEPLOY_ID="$(echo "$RESPONSE" | python3 -c "import json,sys; d=json.load(sys.stdin); r=d.get('result',d) or {}; data=(r.get('data', d.get('data',{})) or {}); print(data.get('deploy_id',''))")"
          if [ -n "$DEPLOY_ID" ]; then
            echo "cPanel deploy queued successfully. deploy_id: $DEPLOY_ID"
          else
            echo "cPanel deploy queued successfully."
          fi
          echo "Track progress in cPanel using log: $LOG_PATH"

          echo "Verifying live site build marker..."
          LIVE_MARKER="$(curl -fsSL --connect-timeout 15 --max-time 30 "https://out.ratib.sa/public/ratib-build.php" 2>/dev/null || true)"
          echo "Live ratib-build.php: ${LIVE_MARKER:-<failed or empty>}"
          EXPECTED="$(cat public/ratib-build.txt 2>/dev/null || echo '')"
          echo "Git expected marker: $EXPECTED"
          if [ -n "$EXPECTED" ] && [ "$LIVE_MARKER" = "$EXPECTED" ]; then
            echo "Deploy verification OK — production matches git marker."
          else
            echo "::warning::Deploy verification FAILED — production still serves old files. In cPanel: Git Version Control → Deploy HEAD Commit manually."
          fi
          exit 0

