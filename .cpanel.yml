---
# Git push → cPanel pulls repo → this syncs checkout to /home/outratib/public_html
deployment:
  tasks:
    - /bin/bash -lc 'export RATIB_PUBLIC_HTML=/home/outratib/public_html; bash scripts/cpanel-deploy-sync.sh'
