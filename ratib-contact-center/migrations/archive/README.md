# Archived RCC migrations

Legacy SQL files from early development. **Do not run** — the migration runner only applies `migrations/*.sql` (this folder is excluded).

Canonical schema: `001_core_schema.sql` through `010_seed_production.sql`.
